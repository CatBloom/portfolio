version: 0.2

phases:
  install:
    commands:
      - yarn install
  build:
    commands:
      - yarn build
  post_build:
    commands:
      - aws s3 sync dist/ s3://${S3_BUCKET_NAME} --delete

artifacts:
  files:
    - '**/*'
  base-directory: dist
